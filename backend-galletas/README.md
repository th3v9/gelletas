# backend-galletas

Pequeño backend Express para la aplicación Galletas.

Instalación y ejecución:

```bash
cd backend-galletas
npm install
npm start
```

El servidor escucha en `http://localhost:3000` y los endpoints están en `/productos`.
